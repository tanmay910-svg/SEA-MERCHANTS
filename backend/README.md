# Backend

API and application layer for SEA MERCHANTS.

Planned responsibilities:
- Validate user inputs
- Expose API endpoints
- Connect frontend to AI/ML services
- Handle persistence/database integration
- Return normalized recommendation and calculation results

The backend should call the AI/ML layer rather than duplicate its model logic.